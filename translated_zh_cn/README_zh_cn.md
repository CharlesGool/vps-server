# <Project Name>

[English](../README.md) | **简体中文**

> 译自 `README.md`（v<release-version>）。如有冲突，以英文版为准。

用一句话说明项目是什么、面向谁。

## 功能

- <能力 1>
- <能力 2>

非目标：<本项目刻意不做什么>

## 要求

- 操作系统 / 运行时：<例如 Linux、Python 3.12>
- 硬件：<仅在有要求时填写>
- 外部服务：<需要的 API、账号>

## 安装

```bash
# 始终克隆 tag，不要克隆可能正在开发中的默认分支。
git clone --branch v0.1.0 --depth 1 <repo-url> <project>
cd <project>
<install command>
# 仅当项目确实有配置变量时执行：
cp .env.example .env   # 填入值，见“配置”
```

## 快速开始

```bash
<最短的有效命令>
```

## 验证是否成功

<可观察、可判断的成功结果。>

## 配置

| 变量 | 含义 | 默认值 | 必填 |
|---|---|---|---|
| `EXAMPLE_KEY` | <用途> | — | 是 |

完整说明：见 `DESIGN.md` 的 Configuration reference。

## 许可证

<private 仓库可省略本节。公开前必须完成许可证与来源复核；写明所选开源许可证，或在未授予开源使用许可时写“保留所有权利”。如包含第三方材料，链接到 THIRD_PARTY_NOTICES.md。>
